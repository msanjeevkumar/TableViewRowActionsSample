<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:local="clr-namespace:registration" x:Class="registration.registrationPage">
    <ContentPage.Content>
        <StackLayout VerticalOptions="FillAndExpand" BackgroundColor="Black">
            <BoxView HeightRequest="10" />
            <Button Text="Add" Clicked="Handle_Clicked" TextColor="Red" BackgroundColor="White" />
            <local:CustomListView x:Name="CartList" BackgroundColor="Black" VerticalOptions="Start" IsVisible="true" SeparatorVisibility="None" HorizontalOptions="FillAndExpand">
            </local:CustomListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>